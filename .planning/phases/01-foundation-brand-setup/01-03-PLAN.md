---
phase: 01-foundation-brand-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - brand_config.yaml
  - tests/test_brand_config.py
autonomous: true
requirements:
  - IMG-02
  - INFRA-02

must_haves:
  truths:
    - "brand_config.yaml contains all PUM brand colors in valid hex format"
    - "brand_config.yaml font paths resolve to existing TTF files"
    - "brand_config.yaml logo paths resolve to existing PNG files"
    - "brand_config.yaml sector icon paths resolve to existing PNG files"
    - "Test script verifies all config values and asset paths in one command"
    - "No secrets or API keys appear in brand_config.yaml"
  artifacts:
    - path: "brand_config.yaml"
      provides: "Complete PUM brand configuration"
      contains: "colors"
    - path: "tests/test_brand_config.py"
      provides: "Automated brand config and asset verification"
      contains: "def main"
  key_links:
    - from: "brand_config.yaml"
      to: "assets/fonts/"
      via: "Font path references in config resolve to actual TTF files"
      pattern: "assets/fonts/.*\\.ttf"
    - from: "brand_config.yaml"
      to: "assets/logos/"
      via: "Logo path references in config resolve to actual PNG files"
      pattern: "assets/logos/.*\\.png"
    - from: "brand_config.yaml"
      to: "assets/icons/"
      via: "Icon directory and filename references resolve to actual PNGs"
      pattern: "assets/icons/"
    - from: "tests/test_brand_config.py"
      to: "brand_config.yaml"
      via: "Test loads config and verifies all paths resolve"
      pattern: "yaml.safe_load"
---

<objective>
Create the brand configuration YAML file and a test script that verifies all config values and asset paths resolve correctly.

Purpose: brand_config.yaml is the single source of truth for PUM brand identity — every image template, color choice, and font reference flows through this file. The test script ensures nothing is broken and serves as a smoke test for CI.
Output: brand_config.yaml at project root, tests/test_brand_config.py that validates everything.
</objective>

<execution_context>
@/Users/anitawulandari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anitawulandari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-brand-setup/01-CONTEXT.md
@.planning/phases/01-foundation-brand-setup/01-RESEARCH.md
@.planning/phases/01-foundation-brand-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-brand-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand_config.yaml with complete PUM brand identity</name>
  <files>brand_config.yaml</files>
  <action>
Create `brand_config.yaml` at project root following the schema from RESEARCH.md. All values are from locked user decisions in CONTEXT.md.

The YAML file MUST contain these sections:

**brand:** Name ("PUM Netherlands Senior Experts") and slogan ("Together we grow" — English only per user decision).

**colors:**
- primary: mint_green (#D2E8D7), dark_green (#0E5555), orange (#FF6900)
- secondary: blue (#659BD1), warm_brown (#D69A5F), soft_gold (#E9C779), light_beige (#F8E3B3)
All hex values uppercase, 7 characters including #.

**fonts:**
- heading: path to Noto Sans Bold TTF, weight 700
- body: path to Noto Sans Regular TTF, weight 400
- decorative: path to Permanent Marker TTF

Font paths MUST match the actual filenames created by Plan 01-02. Reference the 01-02-SUMMARY.md to get exact filenames. Expected: `assets/fonts/NotoSans-Bold.ttf`, `assets/fonts/NotoSans-Regular.ttf`, `assets/fonts/PermanentMarker-Regular.ttf`. If 01-02 used variable fonts instead, adjust paths accordingly.

**logos:**
- primary: `assets/logos/PUM_logo-donkergroen-rgb.png` (dark green, default watermark)
- white_with_slogan: `assets/logos/PUM_logo-slogan-alternatief-wit-rgb_1.png`

**icons:**
- directory: `assets/icons/`
- sectors: Map clean keys to actual filenames for all 22 sector icons. Keys use snake_case (e.g., `agriculture`, `food_beverage`). Values are the exact PNG filenames with special characters (e.g., "Agriculture & Beekeeping.png").
- priority_indonesia: List of 5 priority sector keys: agriculture, food_beverage, hospitality, manufacturing, education

**decorations:**
- directory: `assets/decorations/`
- krabbelbabbel: List of 3 KrabbelBabbel filenames

Use the exact schema from RESEARCH.md code example. All paths relative to project root (no leading /).

MUST NOT contain: API keys, passwords, social media handles, hashtags, content pillars, tone/voice guidelines (per user decisions in CONTEXT.md).

Open YAML file with comment: `# PUM Indonesia Content Generator - Brand Configuration` and `# Source: PUM Canva Premium Brand Kit`.
  </action>
  <verify>
    <automated>python3 -c "
import yaml
from pathlib import Path
with open('brand_config.yaml', 'r', encoding='utf-8') as f:
    config = yaml.safe_load(f)
assert 'brand' in config
assert 'colors' in config
assert 'fonts' in config
assert 'logos' in config
assert 'icons' in config
assert config['colors']['primary']['dark_green'] == '#0E5555'
assert config['colors']['primary']['orange'] == '#FF6900'
assert config['brand']['slogan'] == 'Together we grow'
assert len(config['icons']['sectors']) >= 20
print('brand_config.yaml structure valid')
"</automated>
  </verify>
  <done>brand_config.yaml exists at project root with all PUM brand colors, font paths, logo paths, sector icon mappings, and decoration references — no secrets, no out-of-scope content</done>
</task>

<task type="auto">
  <name>Task 2: Create test script that verifies all brand config values and asset paths</name>
  <files>tests/test_brand_config.py</files>
  <action>
Create `tests/test_brand_config.py` following the test script pattern from RESEARCH.md. This script is the Phase 1 "smoke test" — if it passes, the foundation is solid.

The script MUST verify (in order):

1. **Config loads:** `brand_config.yaml` exists and parses with `yaml.safe_load()`. Open with `encoding="utf-8"` (Pitfall 5 from RESEARCH.md).

2. **Color format:** Every hex color value starts with `#` and is exactly 7 characters. Check both primary and secondary color groups.

3. **Font files exist:** For each font in config, resolve the path relative to project root and assert the file exists on disk.

4. **Logo files exist:** For each logo in config, resolve path and assert exists.

5. **Icon files exist:** Combine `icons.directory` with each sector filename and assert each exists. Print count.

6. **Decoration files exist:** Combine `decorations.directory` with each KrabbelBabbel filename and assert exists.

7. **Font loading with Pillow:** For each font, load with `ImageFont.truetype(str(path), size=24)`. Do NOT attempt variable font axis setting — use fonts at their default weight. If the font loads and `getbbox()` returns a non-None result for Indonesian sample text ("PUM Indonesia — Bersama Kita Tumbuh"), the font is working.

8. **Logo loading with Pillow:** Open each logo with `Image.open()` and call `verify()`.

9. **No secrets check:** Assert the string "API_KEY" does not appear anywhere in brand_config.yaml content. Assert "password" (case-insensitive) does not appear.

Use `Path(__file__).resolve().parent.parent` to resolve project root (test is in `tests/` directory).

Print `[OK]` for each check that passes. Print `\n=== All brand asset checks passed ===` at end. Return 0 on success, 1 on any failure.

Script must be runnable as: `python3 tests/test_brand_config.py`
  </action>
  <verify>
    <automated>python3 tests/test_brand_config.py</automated>
  </verify>
  <done>Test script runs with zero errors, verifying: config parses, all colors valid hex, all font/logo/icon/decoration paths resolve to existing files, all fonts load in Pillow, all logos load in Pillow, no secrets in config file</done>
</task>

</tasks>

<verification>
1. `python3 tests/test_brand_config.py` exits with code 0 and prints "All brand asset checks passed"
2. `python3 -c "import yaml; config = yaml.safe_load(open('brand_config.yaml')); print(len(config['icons']['sectors']))"` prints 22
3. brand_config.yaml contains no API keys or passwords
4. All asset paths in brand_config.yaml resolve to files that exist on disk
</verification>

<success_criteria>
- brand_config.yaml is the complete, authoritative PUM brand configuration
- Every path referenced in the config points to a real file in assets/
- Test script validates the entire config + asset chain in one command
- Phase 1 success criteria from ROADMAP.md are all met:
  1. requirements.txt installs without errors (Plan 01-01)
  2. brand_config.yaml contains PUM brand colors, font paths, and logo path (this plan)
  3. Brand assets exist in assets/ directory (Plan 01-02)
  4. Test script can load brand config and verify all asset paths resolve (this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-brand-setup/01-03-SUMMARY.md`
</output>
