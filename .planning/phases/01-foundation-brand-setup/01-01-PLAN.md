---
phase: 01-foundation-brand-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "pip install -r requirements.txt completes without errors"
    - ".env files are excluded from git tracking"
    - "Expected environment variables are documented in .env.example"
    - "Python cache files and generated output are excluded from git"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependency declarations"
      contains: "PyYAML"
    - path: ".gitignore"
      provides: "Git exclusion rules for secrets and generated files"
      contains: ".env"
    - path: ".env.example"
      provides: "Documentation of expected environment variables"
      contains: "GEMINI_API_KEY"
  key_links:
    - from: ".env.example"
      to: "GitHub Actions secrets"
      via: "Documents variable names that must match GitHub Actions secret names"
      pattern: "GEMINI_API_KEY|GMAIL_APP_PASSWORD"
---

<objective>
Create the Python project foundation: dependency file, git exclusions, and environment variable documentation.

Purpose: All downstream phases need pip dependencies installed and a clean git setup that prevents secrets from being committed. This is the scaffolding everything else builds on.
Output: requirements.txt, .gitignore, .env.example — project is ready for development.
</objective>

<execution_context>
@/Users/anitawulandari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anitawulandari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-brand-setup/01-CONTEXT.md
@.planning/phases/01-foundation-brand-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project dependency and configuration files</name>
  <files>requirements.txt, .gitignore, .env.example</files>
  <action>
Create three files at the project root:

**requirements.txt** — Pin exact versions per research:
```
PyYAML==6.0.3
Pillow==12.1.1
python-dotenv>=1.0.0
```
No extras, no dev dependencies. These are the only libraries needed for v1.

**.gitignore** — Standard Python project exclusions:
```gitignore
# Environment / Secrets
.env
.env.local
.env.production

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
venv/
.venv/

# Generated output
output/
*.generated.png

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
```

**.env.example** — Document ALL environment variables the pipeline will need (INFRA-02: secrets stored as GitHub Actions secrets). These names must match what GitHub Actions secrets will be named:
```
# Google Gemini API
GEMINI_API_KEY=your-gemini-api-key-here

# Gmail SMTP (for email delivery)
GMAIL_ADDRESS=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-app-password-here

# Email recipients
RECIPIENT_EMAIL=team@example.com

# Google Sheets (for content input)
GOOGLE_SHEETS_CREDENTIALS=path/to/service-account.json
GOOGLE_SHEET_ID=your-sheet-id-here
```

Each variable has a descriptive placeholder value. Comments group by service.
  </action>
  <verify>
    <automated>python3 -c "import yaml; import PIL; import dotenv; print('All imports OK')" && test -f .gitignore && test -f .env.example && echo "All files exist"</automated>
  </verify>
  <done>requirements.txt installs all dependencies without errors; .gitignore excludes .env, __pycache__, and generated output; .env.example documents all expected environment variable names matching future GitHub Actions secrets</done>
</task>

<task type="auto">
  <name>Task 2: Verify pip install and git exclusions work correctly</name>
  <files></files>
  <action>
Run verification commands (no files created — this is a validation task):

1. Run `pip install -r requirements.txt` and confirm zero errors
2. Verify each dependency loads: `python3 -c "import yaml; import PIL; import dotenv"`
3. Verify .gitignore works: create a test `.env` file, run `git status`, confirm `.env` does NOT appear as untracked. Remove the test `.env` file after.
4. Print installed versions: `pip show PyYAML Pillow python-dotenv | grep -E "Name|Version"`

If pip install fails on any dependency, check the version pin and update requirements.txt. PyYAML 6.0.3 and Pillow 12.1.1 are the targets; adjust if the exact version is not available on the current platform.
  </action>
  <verify>
    <automated>pip install -r requirements.txt && python3 -c "import yaml; import PIL; import dotenv; print('OK')"</automated>
  </verify>
  <done>pip install completes with zero errors; all three libraries import successfully in Python; .gitignore correctly excludes .env files from git tracking</done>
</task>

</tasks>

<verification>
1. `pip install -r requirements.txt` exits 0
2. `python3 -c "import yaml; import PIL; import dotenv"` exits 0
3. `.env` file does not appear in `git status` output
4. `.env.example` contains GEMINI_API_KEY, GMAIL_APP_PASSWORD, GMAIL_ADDRESS
</verification>

<success_criteria>
- requirements.txt installs PyYAML, Pillow, and python-dotenv without errors
- .gitignore prevents .env files, __pycache__/, and output/ from being tracked
- .env.example documents all expected environment variables with descriptive placeholders
- Project is ready for development — any subsequent plan can assume dependencies are installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-brand-setup/01-01-SUMMARY.md`
</output>
